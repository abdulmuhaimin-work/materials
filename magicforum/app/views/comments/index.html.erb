<div class="comment">
  <h1 class="text-center"><%= @post.title %></h>
  <h1 class="text-center">comments</h1>

  <section class="comments-container">

    <% @comments.each do |comment| %>
      <div class="comment-title text-center">
        <h5><%= comment.body %></h5>
        <%= image_tag comment.image, class: "img-settings img-responsive center-block" %>
      </div>
      <div>
        <%= button_to "edit comment", edit_topic_post_comment_path(@topic, @post, comment), :method => :get %>
        <%= button_to "Delete comment", topic_post_comment_path(@topic, @post, comment), method: :delete, data: { confirm: "Confirm Delete?" } %>
      </div>
    <% end %>

<!--  </section>
  <%= form_for(@comment, url: topic_post_comments_path, html: {id: "comment-create-form" }) do |f| %>
    <h1 class="text-center">
      Create comment
    </h1>
    <div class="form-group">
        <h4 class="text-center"> comment Image </h4>
        <div id="preview-image">
          <% if @comment.image? %>
            <%= image_tag @comment.image, class: "img-settings img-responsive center-block" %>
          <% end %>
        </div>
    </div>
    <div class="form-group">
      <%= f.file_field :image, id: "post_image_field", class: 'form-control image-uploader' %>
    </div>


    <div class="form-group">
      <h4> <%= f.label :text_field, "Body" %> </h4>
      <%= f.text_field :body, required: true, id: "comment_body_field", class: 'form-control' %>
    </div>

    <div class="form-group text-center">
      <%= f.submit "Create Comment", class: "btn btn-success btn-lg" %>
    </div>

  <% end %>-->

  <section class="text-center">
    <%= button_to "create new comment", new_topic_post_comment_path, :method => :get %>
  </section>



</div>
